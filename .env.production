# Used when building the web app for production (Vite mode=production).
# Leave empty when the frontend is served from the same host as the API (single deploy).
# The app will call /api on the same origin. Only set this if you deploy frontend and API separately.
DATABASE_URL="postgresql://neondb_owner:npg_zSt5xQpcdY8I@ep-misty-frog-a4nz9wgn.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
PORT=5000
JWT_SECRET="ForDakamMelody"
VITE_API_URL="http://localhost:5000/api"
VITE_CURRENCY_LABEL=XAF
